recipe_version: '2'
model: Qwen/Qwen3.6-35B-A3B-FP8
runtime: vllm-distributed
builder: eugr
container: vllm-node
metadata:
  description: "Qwen3.6-35B-A3B-FP8 — agentic-coding co-tenant (with 27B), native 256k"
mods:
- mods/fix-qwen3-coder-next
- mods/vllm-tune
- mods/fix-qwen3.6-chat-template
defaults:
  port: 8000
  host: 0.0.0.0
  tensor_parallel: 1
  pipeline_parallel: 1
  # 0.38 × 121 GB ≈ 46 GB. The 27B (at util=0.40) loads first and consumes
  # ~74 GB (48 GB budget + ~25 GB CPU/process overhead), leaving ~47 GB free
  # for 35B's startup check. 0.40 here was 1.4 GB over that ceiling; 0.38
  # fits with margin. Sum 0.40+0.38 = 0.78, still under earlyoom's 80% line.
  gpu_memory_utilization: 0.38
  # 128k context. Trimmed from 200k for more memory headroom.
  max_model_len: 131072
  # Prefill chunk size. Bumping to 32768 was tried (2026-05-14) and
  # produced a slight regression on the 60k benchmark (1583 vs 1627 tok/s),
  # likely because larger activation workspace evicted KV cache. 16384 is
  # the sweet spot for this model on this util budget.
  max_num_batched_tokens: 16384
  load_format: fastsafetensors
  kv_cache_dtype: fp8
  attention_backend: flashinfer
  tool_call_parser: qwen3_xml
  reasoning_parser: qwen3
env:
  VLLM_MARLIN_USE_ATOMIC_ADD: '1'
command: |
  vllm serve {model} \
    --host {host} \
    --port {port} \
    --max-model-len {max_model_len} \
    --max-num-batched-tokens {max_num_batched_tokens} \
    --gpu-memory-utilization {gpu_memory_utilization} \
    --enable-auto-tool-choice \
    --tool-call-parser {tool_call_parser} \
    --reasoning-parser {reasoning_parser} \
    --kv-cache-dtype {kv_cache_dtype} \
    --load-format {load_format} \
    --attention-backend {attention_backend} \
    --enable-prefix-caching \
    --speculative-config '{"method":"mtp","num_speculative_tokens":3}' \
    --chat-template fixed_chat_template.jinja \
    -tp {tensor_parallel} \
    -pp {pipeline_parallel}
