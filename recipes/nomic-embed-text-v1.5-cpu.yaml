recipe_version: '2'
model: nomic-ai/nomic-embed-text-v1.5
runtime: vllm
builder: eugr
max_nodes: 1
container: ghcr.io/spark-arena/dgx-vllm-eugr-nightly-tf5:latest
metadata:
  description: "nomic-embed-text-v1.5 — CPU embedding inference"
defaults:
  port: 8001
  host: 0.0.0.0
  tensor_parallel: 1
  gpu_memory_utilization: 0.3
  max_model_len: 32768
  load_format: safetensors
command: |-
  vllm serve {model} \
    --runner pooling \
    --gpu-memory-utilization {gpu_memory_utilization} \
    -tp {tensor_parallel} \
    --host {host} \
    --port {port} \
    --max-model-len {max_model_len} \
    --load-format {load_format} \
    --trust-remote-code